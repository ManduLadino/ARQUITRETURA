{
  "buildCommand": "npm run build",
  "framework": "vite",
  "installCommand": "npm install",
  "regions": ["gru1"],
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "NODE_ENV": "production"
  }
}
